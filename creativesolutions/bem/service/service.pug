mixin _service(item)
    .layout_col-3
        .service_item
            .service_icon !{item.icon}
            h5.service_title=item.title
            .service_text=item.text
mixin service(rowNeeded,colNeeded)
    - var col = 0;
    - var row = 0;
    //- - var rowNeeded = serviceItems.length / colNeeded;
    //- - var rowNeeded = 2;
    while row < rowNeeded
        .layout_row
            while col < (row + 1) * colNeeded
                +_service(serviceItems[col])
                - col++;
        - row++;